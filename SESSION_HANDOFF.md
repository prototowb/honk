# SESSION_HANDOFF — SPMC

> Read this before anything else. Replace entirely at session end — this is current state, not a log.

## Where We Are

**`feature/BUILD-001-single-origin` — the single-origin build system is COMPLETE.** All four slices shipped: C (consolidation), B1 (tool tables), A (MCP configs), and now **B2 (skills + Hermes triggers generated from a prose source)**. `main` holds BETA-001. Every distribution surface that re-described a machine fact now reads it from one origin.

Read **`BUILD_CONCEPT.md`** for the design. Model: *machine facts* live in `spmc-server/lib/{tools,specs,config}.js` + `spmc-server/package.json`; *prose* is hand-authored in **`capabilities/`**; everything downstream is generated by `build/generate.mjs` and is hand-edit-forbidden (`npm run build`, verified by `npm run build:check`).

## What Shipped This Session — slice B2

**`capabilities/` is now the prose home; `skills/*` + `hermes/SKILLS.md` are generated.**

1. **`capabilities/` source tree (new):** `capabilities/skills/<name>.md` (13) + `capabilities/hermes/SKILLS.md`. This is the only place humans edit skill/Hermes prose now.
2. **Token resolver in `generate.mjs`:** `{{limit:bluesky.text.max}}` → `PLATFORM_SPECS.bluesky.text.max` (300); `{{unit:bluesky.text}}` → `.text.unit` ("graphemes"); `{{tool:bluesky_post}}` → tool name validated against `TOOLS`. Resolution is a 1:1 specs object-path lookup, **no aliases**. Any bad path / unknown tool / unknown token kind / stray `{{` **fails the build** (`resolveTokens` throws). So limits and tool names in prose can't drift from what the server serves.
3. **Generated `skills/*/SKILL.md` (13) + `hermes/SKILLS.md`:** `renderSkill()` validates the frontmatter `name` (Agent Skills spec: ≤64, lowercase/digits/hyphens, no `claude`/`anthropic`), resolves tokens, and splices the `DO NOT EDIT` header **after** the closing frontmatter `---` (never before — `---` must be the first bytes or loaders break). Skills are **discovered** via `readdirSync(capabilities/skills)`, so adding a source file is enough to make a new tracked, build-checked skill.
4. **`output-manager` body reconciled:** dropped the Dashboard/Queue/Scheduling sections (they overlapped `manage-queue`), rewrote the intro to its actual narrowed scope (visual-content generation + pointer to `manage-queue`), completed the pre-existing truncated rule. The protocode logo code is untouched.
5. **`.gitattributes`:** `skills/**/SKILL.md` + `hermes/SKILLS.md` pinned `eol=lf` (verified via `git check-attr`), same LF rule as the other generated artifacts.

**Verified:** `npm run build` idempotent; **`build:check` green on all 21 artifacts**; `npm --prefix spmc-server test` = **38/38**; `test:smoke` = **SMOKE PASS**. The round-trip diff gate held: the 12 token/copy skills diffed **+2 lines only** (the header) — proof every token resolves byte-identically — with deliberate content changes confined to `output-manager` (reconciliation) and `post-to-instagram` (`2,200`→`2200`, raw, to match the B1 tool tables which render `limitFor()` un-separated).

## Generator state (`build/generate.mjs`)

Emits **21 artifacts** via `ARTIFACTS`: `TOOLS.md`, `.claude-plugin/plugin.json`, injected regions in `README.md` + `hermes/CONTEXT.md`, the 3 MCP configs, **13 `skills/*/SKILL.md`**, and **`hermes/SKILLS.md`**. Render kinds: full-file (`render()` returns content), inject (`injectArtifact()`), and prose-source (`renderSkill()` / `renderHermesSkills()` → read `capabilities/*`, `norm()`, resolve tokens, add header). The write branch `mkdirSync`s the target dir so a brand-new skill source materializes its `skills/<name>/` dir. Zero runtime/build deps. **The build now hard-depends on `capabilities/`** (`readdirSync` at module load throws if absent).

## NEXT SESSION — wire-up + merge-back (no slices left)

> Keep `npm --prefix spmc-server test` + `npm run build:check` green at every commit.

- **Wire `build:check` into CI + a pre-commit hook.** One machine-local artifact to handle: `hermes/mcp-config.json`'s absolute path is generated from the build machine's `ROOT`, so CI sees drift unless overridden. Clean fix: env override defaulting to ROOT (e.g. `SPMC_HERMES_ROOT ?? ROOT`).
- **Single-origin the remaining surfaces:** `.env.example` is a *fourth* hand-maintained copy of the same 17 credential keys (same drift class A fixed) — generate it (or a checked block) from `credentialEnvKeys()`.
- **Merge-back into `PROJECT_ARCHITECTURE.md`** (BUILD_CONCEPT's *Merge-back* section lists what moves): the *Single origin + generated distribution* decision; the generated/source split in the directory tree (`build/`, `capabilities/` = sources; `.claude-plugin/`, `skills/`, `.mcp.json`, `hermes/*`, the 3 MCP configs = generated); the surface-viability matrix. Then reduce `BUILD_CONCEPT.md` to a pointer.
- Consider a `0.2.0-alpha` version bump (now single-origin in `spmc-server/package.json`).
- Then **merge `feature/BUILD-001-single-origin` → `main`.**

## Flags / Known-but-out-of-scope

- **Pre-existing content truncations (present since `e31dd68`, the first commit — NOT introduced this session):** `capabilities/skills/pipeline-orchestrator.md` ends on a dangling `2` (the "How to find primary sources" list is cut off); `output-manager`'s "Rules (non-negotiable)" list lost rules 3+ (only rule 1 + the completed rule 2 survive). Preserved verbatim by B2 (byte-faithful migration) except output-manager's rule-2 word-completion. These are **creative-content gaps to fill by hand**, not build bugs — fix in `capabilities/`, then `npm run build`.
- **SKILL.md header placement is reasoned-safe, not load-tested.** The `DO NOT EDIT` HTML comment now sits between the closing frontmatter `---` and the body. Frontmatter parsers read only the fenced block and `---` is still first bytes (same pattern README/CONTEXT use), so this should load — but no skill was actually loaded post-B2 to confirm. If convenient, eyeball-load one skill in Claude Code.

## Still Deferred / Stop-Lines (unchanged)
- **Live credential testing — deferred by user choice.** Nothing has published end-to-end; `analytics_*` / `getMetrics` / `rate_limits` UNVERIFIED against live APIs. Order: Bluesky → X → Meta.
- **UI implementation planning — intentionally NOT started** (the stop-line). Analytics dashboard + calendar = Phase 2/3.

## Conventions In Force
- **Build origin:** change a tool → `lib/tools.js`; a limit → `lib/specs.js`; a credential/media key → `lib/config.js` (or `specs.js` credentials); metadata/version → `spmc-server/package.json`; **skill/Hermes prose → `capabilities/`**. Then `npm run build`. **Never hand-edit generated artifacts** (`TOOLS.md`, `.claude-plugin/plugin.json`, the `gen:tools` regions in `README.md` + `hermes/CONTEXT.md`, the 3 MCP configs, **all `skills/*/SKILL.md`**, and **`hermes/SKILLS.md`**). Hand-editing **outside** the `gen:tools` markers in README/CONTEXT is still fine.
- Server `spmc`; `run.js` = MCP only; `start.js` = MCP + scheduler. One dispatcher (`lib/dispatch.js`); every real publish goes through `publishAudited`.
- Credentials: `~/.claude/spmc.env` (self-loaded by `run.js`); multi-account `KEY__ACCOUNT`.
- Keep the suite green at every commit. Runtime deps still 2; build tooling adds **zero**.

## Open Questions
- Version bump to `0.2.0-alpha` for the expanded surface? (decide at wire-up)
