# SESSION_HANDOFF — SPMC

> Read this before anything else. Replace entirely at session end — this is current state, not a log.

## Where We Are

**`feature/BUILD-001-single-origin` is pushed and in sync with `origin`** (`github.com/prototowb/honk`). `main` holds BETA-001. This session shipped **slice C (consolidation + cleanup)** and **slice B1 (tool-table injection)**. Remaining on the BUILD-001 plan: **B2 → A**, then wire-up/merge-back.

Read **`BUILD_CONCEPT.md`** for the design. Model: *machine facts* live in `spmc-server/lib/tools.js` + `lib/specs.js` + `spmc-server/package.json`; *prose* is hand-authored; everything downstream is generated by `build/generate.mjs` and is hand-edit-forbidden (`npm run build`, verified by `npm run build:check`).

## What Shipped This Session

1. **Slice C — one SPMC plugin** (`fb5b1d6`): moved the 4 `content-pipeline` skills into top-level `skills/` (now **13 skills**), dropped `test-skill`, deleted the second plugin identity (`content-pipeline/.claude-plugin/plugin.json`) + the wrapper. Fixed skill frontmatter to kebab `name:`. **Narrowed `output-manager`** to its unique value (platform visuals + protocode logo) so it no longer collides with `manage-queue`'s triggers (body prose left for B2). Folded the Path A/B workflow into README's new **Content Pipeline** section.
2. **Slice C cleanup** (`38b539c`): `git rm`'d `_bkp/` (68 MB) and `social-publisher/` (user-confirmed). 9,198 deletions.
3. **Slice B1 — tool tables generated + injected** (`bc48531`): the 23-tool tables in `README.md` and `hermes/CONTEXT.md` are now generated from the origin and injected between `<!-- gen:tools:start/end -->` markers. New generator pieces: `renderToolTables(heading)` (shared with `TOOLS.md`) + `injectArtifact(host, marker, body)` (reads host → replaces region → returns whole file; missing markers throw). Curated prose kept **outside** the markers (Notes blocks, Gotchas, Return Values, Queue Format). Migrated the two schema-absent X facts into Gotchas first (`URLs = 23 chars` → README; `emoji > U+FFFF = 2` → CONTEXT). Neutralized 5 hardcoded "23" tool-counts in README (drift-prone inside a now-generated file). Pinned README + CONTEXT to LF in `.gitattributes`.

**Verified:** `npm run build` idempotent; `build:check` green on all 4 artifacts **and** exits 1 on a perturbed injected region (tested, not just clean path); `npm --prefix spmc-server test` = 38/38.

## Generator state (`build/generate.mjs`)

Emits **4 artifacts** today via the `ARTIFACTS` registry: `TOOLS.md`, `.claude-plugin/plugin.json`, and injected regions in `README.md` + `hermes/CONTEXT.md`. Two render kinds: full-file (`render()` returns content) and inject (`injectArtifact(...)` returns a closure). `norm()` makes the whole-file compare CRLF-insensitive and is reused to normalize host files before injection. Zero runtime/build deps.

## NEXT SESSION — execute in this order: B2 → A

> Keep `npm --prefix spmc-server test` + `npm run build:check` green at every commit; commit + push per slice.

### B2 — `capabilities/` prose layer; make `skills/` generated (the big lift — checkpoint before starting)
**Goal:** stop hand-maintaining `skills/*/SKILL.md`. Prose moves to a hand-authored `capabilities/` tree; the generator emits `skills/*/SKILL.md` with tokens resolved against the origin.
**Steps:**
- Create `capabilities/` as the prose home for the 13 skills + injectable fragments. Prose carries tokens: `{{tool:bluesky_post}}`, `{{limit:bluesky.text.max}}`, `{{unit:bluesky.text}}`.
- Token resolution = **specs object path 1:1**: `{{limit:bluesky.text.max}}` → `PLATFORM_SPECS.bluesky.text.max`; `{{unit:bluesky.text}}` → `.text.unit`. (The existing `limitFor()` already reads `.max`/`.unit`.) Don't invent aliases.
- Emit `skills/*/SKILL.md` from `capabilities/`; add them to `ARTIFACTS`; `build:check` then guards the whole `skills/` tree.
- Also fold **`hermes/SKILLS.md`** into this pass (or its own) — it still hand-carries per-platform limits + the `emoji > U+FFFF` fact (line ~15). Deliberately deferred from B1, not forgotten.
- Reconcile `output-manager`'s body (B1 only narrowed its frontmatter; the body still has Dashboard/Queue/Scheduling sections that overlap `manage-queue`).
**Design constraints (decided, don't rediscover):**
- **The `DO NOT EDIT` header cannot precede SKILL.md frontmatter** — the `---` must be the first bytes or skill loaders break. Put the header *inside* frontmatter (a comment/field) or after it, never before. (SKILL.md cousin of the "JSON can't carry comments" constraint.)
- The **4 pipeline skills** (`idea-input`, `research-trends`, `pipeline-orchestrator`, `output-manager`) are token-free pure prose → copy-through; they won't exercise the token machinery, that's fine.
- Inject `render()` returning the whole file already gives `build:check` coverage for free — reuse that shape.

### A — MCP configs from one template (smallest, do LAST)
**Goal:** `.mcp.json`, `hermes/mcp-config.json`, `claude_desktop_config.json` all rendered from one parameterized template.
**Steps:**
- Export `MEDIA_PROVIDERS` from `spmc-server/lib/config.js` (currently module-local). Env-var list = `flatten(PLATFORM_SPECS[*].credentials)` + `flatten(MEDIA_PROVIDERS)` (= the 17 keys hand-listed in `.mcp.json`).
- One template fn → 3 targets: `.mcp.json` (`${CLAUDE_PLUGIN_ROOT}/spmc-server/run.js` + env block), `hermes/mcp-config.json` (absolute `ROOT`, **no env — creds self-load via run.js**), `claude_desktop_config.json` (`npx -y spmc`).
- **JSON artifacts can't carry the DO-NOT-EDIT comment** — rely on `build:check` + pin in `.gitattributes`. Don't add marker keys a schema validator might reject. Expect cosmetic key-order churn vs the hand-formatted files; generated becomes canonical.

### After A — wire it up + merge back
- Wire `build:check` into CI + a pre-commit hook (CRLF fix was the prerequisite, now done).
- Consider a `0.2.0-alpha` bump (single-origin in `package.json`). NOTE: `PROJECT_STATUS.md` still carries a point-in-time "23 tools" metric — that's a status snapshot, not a generated file, so it's fine, but bump it if the surface grows.
- Fold the **Single origin + generated distribution** decision into `PROJECT_ARCHITECTURE.md`; reduce `BUILD_CONCEPT.md` to a pointer (its *Merge-back* section lists what moves).

## Still Deferred / Stop-Lines (unchanged)
- **Live credential testing — deferred by user choice.** Nothing has published end-to-end; `analytics_*` / `getMetrics` / `rate_limits` UNVERIFIED against live APIs. Order: Bluesky → X → Meta.
- **UI implementation planning — intentionally NOT started** (the stop-line). Analytics dashboard + calendar = Phase 2/3.

## Conventions In Force
- **Build origin**: change a tool → `spmc-server/lib/tools.js`; change a limit → `lib/specs.js`; change metadata/version → `spmc-server/package.json`. Then `npm run build`. Never hand-edit generated artifacts (`TOOLS.md`, `.claude-plugin/plugin.json`, the `gen:tools` regions in `README.md` + `hermes/CONTEXT.md`, and — after B2/A — `skills/*`, MCP configs). Hand-editing **outside** the `gen:tools` markers is fine and expected.
- Server `spmc`; `run.js` = MCP only; `start.js` = MCP + scheduler. One dispatcher (`lib/dispatch.js`); every real publish goes through `publishAudited`.
- Credentials: `~/.claude/spmc.env` (self-loaded by `run.js`); multi-account `KEY__ACCOUNT`.
- Keep the suite green at every commit. Runtime deps still 2; build tooling adds **zero**.

## Open Questions
- Version bump to `0.2.0-alpha` for the expanded surface? (defer to "after A")
- B2: does `capabilities/` hold full skill prose (skills/ becomes 100% generated) or fragments only? Lean full-prose per BUILD_CONCEPT, but confirm at B2 kickoff.
